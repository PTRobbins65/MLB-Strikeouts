# MLB Strikeout Prediction Pipeline — Requirements

# Core data
pybaseball==2.2.7           # Statcast + FanGraphs scraping
requests==2.31.0

# Data processing
pandas==2.1.4
numpy==1.26.4
pyarrow==14.0.2             # Parquet I/O (fast cache)

# Model
scikit-learn==1.4.2
xgboost==2.0.3
joblib==1.3.2               # model serialization (Poisson GLM)

# API server
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# Object storage (R2 / S3 / B2) — used only when STORAGE_BACKEND=s3
boto3==1.34.84
