# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Virtual environments
venv/
.venv/
env/

# Secrets — never commit real credentials (.env.example is the safe template)
.env
.env.*
!.env.example

# Data cache (large files, regenerated by the pipeline)
data/raw/
data/processed/
data/features/
data/storage/
data/player_id_map.parquet

# Accuracy log — live data file, not regenerated (keep out of git)
data/accuracy_log.parquet

# Trained models — committed to repo so Railway can serve predictions.
# Re-run model_trainer.py locally and push when retraining.
# models/

# Logs
logs/

# OS
.DS_Store
Thumbs.db
